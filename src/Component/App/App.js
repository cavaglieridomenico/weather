import { Container } from 'react-bootstrap';
import UploadFile from '../UploadFile';

const App = () => {
  return (
    <Container>
      <UploadFile />
    </Container>
  );
};

export default App;
